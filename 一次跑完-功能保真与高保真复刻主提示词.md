# 小喜鹊冠心病康复小程序：一次跑完的功能保真与高保真复刻主提示词

> 使用方式：把本文件全文作为一个任务提示词交给 Coding Agent。不要拆成多个对话，不要只做分析或计划；要求智能体在同一任务内持续实施、验证、修复并交付。

> 提示词核校版（2026-09-07）：本文件是交给后续执行者的实施指令，不代表本次写作已完成代码或验收。配套必读 `docs/复刻执行细则与证据清单.md`，包含28页直接参考链接、字段口径、失败恢复、逐页证据和无人值守处理规则。两份文件属于同一执行合同；如措辞仍与只读本地基线的实际行为冲突，先记录证据并按功能基线裁决，不能因为提示词写得肯定就篡改业务。

> 2026-09-07 实测修订：当前副本已有实施与回归证据，请从现状继续，不要再次覆盖为初始版本。先读取同目录 `docs/REPLICA-DECISIONS.md`、`docs/REPLICA-REVIEW.md`、`docs/visual-acceptance/latest/visual-diff-report.md`。这些文件记录实际功能冲突与剩余差异；本地原项目依然是功能权威。当前状态为 REVIEW_COMPLETE_WITH_GAPS，不能把已生成28张运行图或自动检查通过当成完美交付。

> 可复用的本轮资产和组件：`src/components/PrescriptionTaskCard.vue`、`SessionReportSummary.vue`、`RewardProductCard.vue`；助手已有独立滚动与常驻输入栏。五类礼品插图、完整生成提示词和源路径见 `docs/REPLICA-REWARD-ASSETS.json`。保留这些经过回归的实现，按逐页差异继续精修。静态PNG展示3D风格，不引入重型3D运行引擎。

> 续接补充：运动选择已抽出 `src/components/ExerciseSelection.vue`；`KnowledgeHub.vue` 与 `CompanionHub.vue` 已有资讯重排，相关四张新素材见 `docs/REPLICA-KNOWLEDGE-ASSETS.json`，相关验收脚本为 `scripts/replica-catalog-qa.cjs`。旧评审汇总可能尚未纳入最新结果，必须重核时间及实际输出，不能只复制旧总数。

> H5键盘续接：保留 `src/lib/prototype-keyboard.ts` 与H5条件导入的 `H5KeyboardDate.vue`。适配器只调用原事件，不拥有业务状态。尤其不能删除Enter/Escape已处理松键的隔离，否则可能出现“打开兑换弹窗即自动确认”。详细复测要求见配套细则第11节；结果从 `keyboard-run.json` 读取，不预填PASS。当前构建成功仍不证明包体可发布、原生读屏或视觉一比一。

> 独立分发续接：`scripts/replica-runtime-delivery.cjs`与`replica-delivery-qa.cjs`已实现源/编译引用闭包、三端独立副本、逐文件hash/HTTP/视频Range验证。当前目录从`docs/visual-acceptance/latest/runtime-delivery.json`读取，不硬编码历史run目录；生产浏览器证据在`docs/visual-acceptance/runtime-delivery/`，不能与默认开发版证据混用。源与原构建素材全部保留，不能再次按“未使用”删除。保留H5远程阴影预加载修复以及P05周期文字对比度修复；平台分包与真机仍须独立验证。

---

## 一、你的角色与最终目标

你是一名同时具备资深产品、移动端 UI/UX、UniApp/Vue 3、微信小程序、H5、可访问性、视觉回归测试和医疗健康产品安全意识的高级工程智能体。

请在下面指定的“实施目标副本”内，完整升级“小喜鹊运动伙伴”冠心病居家康复原型：

1. **百分之百保留现有项目的功能范围、状态模型、数据规则、角色分流、训练闭环、异常阻断、报告、设备、知识、积分、社交、医生审核、持久化和多端构建能力。**
2. **依据 28 张最新高保真效果图逐页复刻视觉层、信息层级和组件布局，目标视口为 390 × 844。**
3. **把所有“小喜”角色统一为紫色小喜鹊，绿色只作为产品 UI 主色；不得出现绿色小喜鹊。**
4. **所有界面必须是真实可交互、可运行、可构建的 UniApp 页面，禁止把整张效果图当背景图伪装成界面。**
5. **一次执行到底。不要停在计划、分析、半成品、静态截图或“等待用户确认”。完成实现后必须自行运行、逐流程操作、发现问题、修复、回归，再给最终报告。**

最终完成标准不是“看起来差不多”，而是：功能无回退、主要视觉高度一致、全部关键动作可到达、H5 真浏览器可运行、构建通过、没有控制台错误、没有静态假交互、没有医疗边界错误。

---

## 二、授权范围与自动决策规则

用户现在不会即时回复。你应在以下范围内自主执行，不要因普通实现问题、样式取舍、包版本问题或测试失败停下来提问：

- 可以读取、编辑、重构和测试“实施目标副本”内的代码、样式、资源、文档和本地测试数据。
- 可以运行依赖安装、格式化、类型检查、单元/运行时测试、H5/微信小程序/App 构建、开发服务器和浏览器验收。
- 可以创建本地 QA 截图、视觉对照图、测试记录和交付说明。
- 可以修复构建问题、视觉问题、交互问题、响应式问题、资源路径问题和状态持久化问题。
- 可以在不改变业务含义的前提下拆分过大的 Vue 文件、抽取组件、tokens、composables 和测试辅助代码。
- 不要推送 GitHub，不要创建 PR，不要部署线上，不要连接真实医院、真实设备、真实支付、真实消息或真实健康数据。
- 不要修改“功能基线原项目”和“效果图目录”；它们只读。
- 不要执行破坏性删除；旧资源暂时不用时保留或明确归档，不要为了清理而丢失能力。

遇到歧义时，不要询问用户，按下面“权威顺序”决策并在最终报告记录：

1. 现有源代码、类型、状态转换和本地数据规则决定**功能真相**。
2. 28 张最新效果图决定**视觉真相**。
3. `00-设计方案与GitHub一致性清单.md` 决定**页面覆盖与映射真相**。
4. 现有 README、决策记录与迁移文档决定**产品边界和历史约束**。
5. 在线站点、GitHub 页面与小红书案例仅作补充，不得覆盖前四项。

若效果图中的演示数字、医院名、日期、导航文案或训练时长与源代码的动态状态冲突，必须遵循：

- **功能、真实运行数据、状态和医学语义以源代码为准。**
- **位置、层级、留白、色彩、圆角、图标风格、插画比例和视觉节奏以效果图为准。**
- 可以让种子数据在不破坏规则时接近效果图，但不能为了截图硬编码运行结果。
- 禁止把效果图中的 `8000 步`、`135 积分`、示例医院名等写死，覆盖现有 `DAILY_STEP_GOAL`、钱包、共享患者 fixture 等真实原型状态。

普通报错先自主排查、修复和重试。遇到必须新增授权、不可恢复覆盖、真实数据/账号/付费操作或平台能力缺失时，不擅自扩大范围：隔离受影响项，继续其余安全工作；全部可做事项完成后准确交付剩余缺口。不得绕过权限、伪造测试结果或无限重复同一失败。“用户睡觉无需等待”只授权范围内的合理实现决策，不是外部操作的无限授权。

---

## 三、输入材料与直接参考地址

### 3.1 实施目标副本（只在这里写代码）

`/Users/guagua/Documents/冠心病课题/小喜鹊精简功能版-原型副本`

### 3.2 功能基线原项目（只读，用于前后功能等价性比较）

`/Users/guagua/Documents/冠心病课题/小喜鹊精简功能版`

两者初始 Git 基线应为：

`c1b56ff43a94c0e7e096bb80f0f19f998673a48c`

### 3.3 最新视觉效果图目录（只读）

`/Users/guagua/Documents/冠心病课题/output/小喜鹊原型升级-20260906`

页面映射清单：

`/Users/guagua/Documents/冠心病课题/output/小喜鹊原型升级-20260906/00-设计方案与GitHub一致性清单.md`

### 3.4 可直接打开的参考页面

- 当前本地运行页：`http://127.0.0.1:4173/`
- 在线旧版 H5：<https://lxxpluto-lang.github.io/>
- GitHub 功能基线：<https://github.com/lxxpluto-lang/lxxpluto-lang.github.io>
- 绿色自然系小程序视觉参考：<https://www.xiaohongshu.com/explore/6974f554000000001a028a85>
- 花店/植物系小程序视觉参考：<https://www.xiaohongshu.com/explore/68ecce660000000003037aef>

小红书链接可能出现登录遮罩。不要为此阻塞，也不要要求用户登录；本地 28 张效果图是更高优先级、足够完整的视觉依据。

### 3.5 需要主动检查的现有文档与核心文件

- `README.md`
- `docs/DEC-AESTHETIC-001-京东健康参考视觉基线.md`
- `docs/DEC-AESTHETIC-002-小喜鹊图标系统.md`
- `docs/SHANBAY-MIGRATION-20260901.md`
- `docs/RIVE-HANDOFF.md`
- `docs/RIVE-V3-PRODUCTION.md`
- `src/pages/index/index.vue`
- `src/pages/index/index.scss`
- `src/App.vue`
- `src/lib/prototype-data.ts`
- `src/lib/shared-patient.ts`
- `src/lib/rive-motion.ts`
- `src/components/AssistantPanel.vue`
- `src/components/CompanionHub.vue`
- `src/components/KnowledgeHub.vue`
- `src/components/RehabGardenPanel.vue`
- `src/components/TrainingExperience.vue`
- `src/components/TrainingReports.vue`
- `src/components/MagpieMotion.vue`
- `src/components/CameraPreview.vue`
- `src/components/VitalForm.vue`
- `src/pages.json`
- `src/manifest.json`
- `package.json`

---

## 四、开始编码前必须完成的 Stage 0 盘点

不要打开文件后立刻凭感觉重写。先在本地完成一次可追溯盘点，并保存到：

`docs/REPLICA-STAGE0-INVENTORY.md`

盘点至少包含：

1. 当前 Git 提交、工作区状态、Node/npm 版本、现有依赖和可运行命令。
2. 所有主导航、详情视图、弹窗、状态、动作、表单字段、持久化字段和数据来源。
3. `DetailView`、`TrainingStatus`、`UserMode`、`AssessmentMode`、`DataSource`、`DataQuality`、`AdviceLevel`、`ReviewStatus` 的完整枚举。
4. 每个 `data-testid` 的页面归属、动作、预期结果和是否保留。
5. 现有 9 个运动项目、5 个运动分类、知识条目、患者处方、奖励项目、社交状态和设备状态。
6. 现有 MP4、poster、sprite、Rive 源文件、WASM/微信原生组件及回退机制。
7. 28 张效果图分别对应哪个运行视图或状态。
8. 每个存量页面的处置方式：`adopt_page`、`inherit_layout`、`rebuild_interaction` 或 `reuse_component`。不得无理由 `discard`。
9. 记录效果图与当前实现之间的差异，但不能把差异直接解释为允许删功能。

已有 Stage 0 时先核对路径、提交和文件变化，沿用历史快照并补充增量记录，不覆盖改造前证据。完成盘点后直接实施，不等待普通样式问题的确认。

---

## 五、绝对不能丢失的功能基线

以下不是建议，而是回归底线。只要有一项丢失、变成静态装饰、无法到达或刷新后错误，即视为未完成。

### 5.1 首次进入与双模式

- 首次进入展示“选择使用方式”。
- 支持“日常运动”与“心脏康复”两条角色路径。
- 日常运动无需患者号即可进入。
- 心脏康复必须经过医院/患者号关联步骤。
- 患者号支持现有 `256572` / `P-256572` 规范化与匹配规则。
- 覆盖 idle、loading、matched、error 状态。
- “健康助手与医疗边界”弹窗必须可打开、可关闭。
- 角色切换/重置逻辑必须保留。

### 5.2 五栏主导航

保留现有五个导航状态和顺序逻辑：

- `today`：今日
- `data`：训练/数据
- `assistant`：小喜
- `discover`：资讯
- `profile`：我的

中间“小喜”入口可视觉突出，但必须继续使用紫色角色。导航高亮不能只靠颜色，应同时有形状、图标状态或文字强调。

### 5.3 今日首页：公众模式与患者模式

- 顶部问候、日期和角色名称动态显示。
- 今日核心任务只能有一个明确主行动按钮。
- 公众模式支持今日自选运动、完成状态和运动分钟。
- 患者模式显示医院处方、版本、医生、处方完成数和轮播/分页。
- 今日步数显示来源、同步时间、目标和进度；缺失值不能显示为 0 或“正常”。
- 显示本周训练、今日运动和连续打卡。
- 首页可到达训练、设备、健康档案、本周路径、小菜园和报告。
- 完成核心任务后主按钮应切换到查看报告，不要仍显示重复开始。
- 自选运动不得被误标为医院处方，不得影响患者处方完成口径。

### 5.4 运动分类与项目

保留 5 个分类：

1. 传统养生
2. 有氧耐力
3. 力量抗阻
4. 柔韧平衡
5. 呼吸放松

保留 9 个项目及现有 ID、分类、时长、交互方式和资源映射：

1. `baduanjin` 八段锦
2. `taichi` 太极舒展
3. `walking` 步行节律
4. `power-bike` 功率车
5. `resistance` 抗阻训练
6. `stretch` 坐姿拉伸
7. `balance` 扶椅平衡
8. `music` 音乐律动
9. `breathing` 呼吸放松

公众模式应能浏览分类、选中运动并开始。患者模式必须区分医院处方与非处方自选运动。

### 5.5 训练前状态检查

- 保留训练前/跟练/训练后三步指示器。
- 支持策略 `off / optional / required`。
- 保留源症状字典及训练前“不适”安全入口；不得为了图中的多选外观虚构已存在的完整症状表单。危险症状文案及阻断动作保持。
- 保留心率、血氧、收缩压、舒张压、不适评分、Borg、数据来源、采集时间和质量。
- 支持设备读取和无有效设备数据时的手动填写。
- 必填字段未完成时不能错误进入训练。
- 出现危险症状时必须阻断开始训练，不能只 Toast 后继续。
- 缺失、过期、拒绝授权和无数据必须有明确可见状态。

### 5.6 跟练与三种交互体验

- `camera-score`：动作跟随/摄像头状态/动作评分。
- `rep-game`：重复次数、连续次数或托举互动。
- `rhythm-game`：节拍命中、总次数、连击和轮次。
- 训练状态完整保留：`idle / checking / active / paused / stopped / completed`。
- 正常训练显示计时、目标时长、活动内容、实时反馈和主要动作。
- 有设备且运动支持时显示实时心率、血氧和更新时间。
- 暂停后计时停止，继续后恢复；不可重复启动多个计时器。
- 保留源状态与标记：正常完成和快速演示均为 completed，快速演示另标记 demoCompleted；安全停止为 stopped 并保留原因。源活动状态中的“提前结束”走 finishTraining(false)，仍可能完成打卡和既有奖励，不得将“提前结束”擅自重定义为安全停止。
- 保留 Rive 启用开关及 MP4 安全回退。缺少正式 `.riv` 时不得破坏现有视频播放。
- H5/App 与微信小程序的播放路径都要保留。

### 5.7 暂停与安全停止

- 暂停页面必须与 `28-训练暂停状态.png` 对齐。
- “继续训练”恢复同一训练，不重置进度。
- “结束本次运动”沿用源 `finishTraining(false)`；它与“不适安全停止”不是同一动作。暂停后仍按该按钮原处理器运行，不把普通结束统一改成 stopped。
- “我有不适”打开 `27-安全停止提示.png` 对应的模态层。
- 模态层至少包含前三个高优先级症状和返回训练。
- 选择症状后立即停止并记录 `stoppedReason`。
- `stopTraining` / `stopBeforeStart` 进入 stopped，不新增完成积分或自动打卡；不要把 `finishTraining` 的 completed 路径一并排除。对异常建议、演示完成、普通结束分别记录实际奖励结果。
- 明确显示：停止不会扣除已有积分。
- 症状持续或加重时提示联系医护人员/急救服务，但不进行诊断。

### 5.8 训练后状态、评估、规则化建议

- 按策略支持训练后关闭、可选、必填。
- 保留心率、血氧、Borg、轻松/适中/较累和异常症状。
- 支持设备同步与手动记录。
- 形成 `TrainingAssessment`：pre、post、complete/partial/missing、comparison、policyVersion。
- 形成 `AIAdvice`：stable、attention、stop、insufficient 四级；包含结论、依据、下一步、来源、规则版本和医疗边界。
- 建议是规则化原型输出，不能宣称诊断、治疗、药物调整或自动修改处方。
- 需要调整患者计划的建议进入医生审核，不得直接改处方。

### 5.9 单次报告、阶段报告与康复小报告

- 保留本次运动报告：运动时长、评分、Borg、前后指标、来源、完成/停止状态和小喜总结。
- 保留 `training-reports` 的“单次报告”自然日汇总列表/详情及月度阶段列表/详情；单条 session 的本次运动报告是另一层，不能混并。
- 每日汇总基于自然日，停止记录要保留但不计为完成。
- 月度阶段报告只有在有效训练日达到既有阈值时生成结论；不足时显示“继续积累/有效训练日不足”。
- 缺失指标显示“未采集”，不能使用 0 替代。
- 新增/实现 `05-康复小报告.png` 的小报告体验，并与本地训练记录联动：近7/30天周期、完成次数、有效训练日、运动前心率首末记录、饮食/心率/居家运动/安全提醒。运动前测量不等同静息测量，不标注静息心率改善；无足够有效记录显示不足。
- 康复小报告不能出现奖杯、奖状、金牌、金色庆功视觉。
- 小报告必须显示“待医生确认/不替代正式诊疗意见”边界；如果已有医生审核状态，应可关联展示。

### 5.10 训练数据

- 展示累计运动时间、完成项目、连续天数和最新解读。
- 身体指标展示数值、单位、来源、质量和更新时间。
- 今日步数、目标、来源与近 7 日趋势保持动态。
- 有效趋势不足两天时不能伪造走势，应显示数据不足。
- 最近 7 次运动显示日期、开始时间、时长、类型和状态。
- 训练数据与报告、设备、档案之间导航闭合。

### 5.11 小喜健康助手

- 从空白聊天升级后的任务型能力必须保留。
- 至少覆盖：今天怎么练、数值怎么了、报告怎么看、计划要调整吗。
- 每项回答结构为：结论、当前状态、判断依据、下一步行动、来源/医疗边界。
- 快捷入口要能真实跳转到今日训练、报告、设备来源或健康档案。
- 自由输入与发送保持可用，空输入不提交。
- 所有出现的 mascot 均为紫色小喜鹊。
- 明示“小喜只做健康信息整理，不替代医生诊断”。

### 5.12 康复资讯、文章和视频

- 保留受角色范围过滤的知识内容。
- 保留精选指南、提示和视频分类。
- 文章详情显示完整正文和“待医学审核/不替代诊断或处方”边界。
- 视频详情必须使用真实 `<video>`/UniApp video 控件，可播放本地素材，有 poster、播放控制和失败回退。
- 不能把视频效果图作为静态视频替代。
- 资讯页原健康小队/健康搭子入口保持 disabled/暂未开放；已新增的“我的→扩展界面演示”作为独立本地预览入口，保证22/23页可验收，不改变原业务开放状态。

### 5.13 健康档案

- 患者模式保留医院记录、处方版本、医生、风险/阶段、只读医疗字段和来源。
- 公众模式保留姓名、出生日期、性别及年龄计算和校验。
- 公众健康目标支持：建立运动习惯、支持健康减重。
- 患者健康目标由共享处方决定，小喜不能自行修改。
- 支持保存个人目标和设备授权相关设置。
- 明示数据用途、来源与隐私。

### 5.14 设备与数据来源

- 保留 Apple健康、Health Connect、智能手环/血氧仪/演示设备入口。
- 手机只负责授权和汇总，不能描述为血氧仪。
- 支持原有模拟连接、同步、来源标记、时间、数据质量及手动步数补录；没有系统级授权证据时只显示模拟状态，不写“已获得系统授权”。
- 数据来源优先级保留：医院/直接设备 → Apple健康/Health Connect → 手动录入 → 演示设备。
- 设备同步后优先于手动记录；缺失不显示为 0。
- H5/小程序继续使用模拟适配器，不读取真实健康数据。
- 原有同步、连接和手动录入有可见结果并刷新保持。完整系统授权开关与撤销并非基线已实现能力，不为复刻效果图擅自接入或虚构；如需新增本地权限演示，单独标明模拟且不能替代真实平台验证。

### 5.15 本周路径

- 按当前自然周动态显示周一至周日。
- 已完成、今天、未来三种状态可区分且不只靠颜色。
- 处方/推荐运动状态与实际打卡、训练记录一致。
- 明示“按状态完成即可，不需要为了积分额外加量”。
- 可从本周路径直接开始今天建议的项目。

### 5.16 运动小菜园

- 保留“成长”和“打卡”两个页签。
- 成长依据为每天第一次有效运动；同一天重复运动不重复成长。
- 停止或未完成训练不成长。
- 每 7 个有效训练日收获一棵，漏训不倒退。
- 显示本轮开始、周期天数、有效运动天数、已收获棵数和剩余天数。
- 打卡页显示真实自然月月历、前后月切换、已打卡日、今天、未来日期、最近记录。
- 刷新后打卡、小菜园进度和月历保持一致。
- **视觉强制：只展示完整树体；不得出现土、花盆、地面、地台、山丘、岛屿、景观底座、橙色沙漠背景。**
- 可使用薄荷色抽象光晕，但不能把光晕做成地面。
- 明示“成长代表运动打卡，不代表康复疗效”。

### 5.17 健康小队与健康搭子

- 小队支持创建、输入邀请码加入、演示加入、成员状态、今日进度、单日温和提醒和邀请码复制/展示。
- 搭子支持 7 天/30 天周期、演示配对、双方今日状态和单日温和提醒。
- 申请、配对、提醒均为本地原型，不发送真实消息。
- 漏打卡不解除关系、不扣积分。
- 只分享“今日已打卡/待打卡”和连续天数，不共享健康档案。
- 不按运动量、体重、心率、血氧、Borg、康复指标或处方完成量排名。
- 刷新后组队、搭子、周期、提醒状态保留。

### 5.18 健康积分、权益与 MEM

- 保留健康积分钱包、奖励台账、兑换记录和 audience 过滤。
- 完成当天核心有效运动时自动打卡并奖励 5 积分；同一天不重复奖励。
- 安全停止 stopped 不新增完成积分或自动打卡；源 finishTraining 的快速演示与活动状态提前结束会进入 completed 并执行既有奖励，必须保留。MEM完整八式连续挑战另有严格条件，快速演示/提前结束不满足该挑战奖励。不得把这些不同奖励口径混为一谈。
- 保留 7/30/60/100 天里程碑。
- 中断不扣除已有积分。
- 营销浏览、拉新和付费任务不计入健康积分。
- 兑换只使用本地演示积分/M币，不接人民币支付或充值。
- 保留 MEM 邀请码解锁、100 健康积分转换 5 M币、八段锦连续挑战和奖励规则。
- 兑换按钮根据余额真实启用/禁用，并记录兑换结果。
- 紫色小喜鹊可以用于视觉陪伴，但不能使用奖杯或金色庆典隐喻疗效。

### 5.19 训练状态策略与医生审核

- 策略仅作用于患者计划，普通用户固定关闭。
- 支持训练前/训练后 `off / optional / required`。
- 保留采集字段开关：心率、血氧、异常症状、Borg、运动后感受、血压。
- 保留来源优先级、版本、草稿/已发布、发布时间和发布新版本。
- 保留稳定、需关注、危险症状、数据不足四类一键演示数据。
- 医生审核队列显示待审核建议、时间、建议变更和状态。
- 支持同意调整、维持计划、驳回；动作后卡片状态持久可见。
- 审核同意按源逻辑更新 `planAdjustment` 展示说明及审核状态，不自动改写共享处方 fixture 的项目、时长或版本；未确认更不能修改。
- 医生工作台仍是本地演示，不能暗示真实医疗操作已发生。

### 5.20 本地持久化、迁移和重置

- 保留 `magpie-prototype-state` 存储键或提供向后兼容迁移。
- 当前源项目保存版本为 schemaVersion 7；旧 schemaVersion 3 数据可读取，并按源逻辑重新保存为7。
- 按原实际规则兼容旧数据：旧points可保留；空或缺失checkIns回落6条演示打卡，单独checkInDate/streak不是完整迁移。旧版步数也可能重建演示记录。应明确展示和测试这种回退，不能声称不存在的无损迁移已经完成。
- 角色、健康目标、个人档案、训练记录、报告、步数、设备、策略、审核、小队、搭子、提醒、积分、兑换、MEM 和打卡均按现有规则持久化。
- 刷新后状态保持；损坏/缺失存储应安全回退到默认数据，不白屏。
- “重置原型”应恢复默认本地状态，不影响项目文件。

---

## 六、28 张效果图逐页复刻合同

所有图位于：

`/Users/guagua/Documents/冠心病课题/output/小喜鹊原型升级-20260906/`

逐张打开原图核对，不得仅凭文件名猜布局。每个效果图都必须映射到一个真实可达运行状态，并在最终 QA 中生成同名实机截图。

### 01 今日首页

参考：`01-今日首页.png`

- 复刻问候区、今日康复大卡、环形进度、指标子卡、医生处方卡、步数卡、快捷入口和五栏导航的层级。
- 紫色小喜位于步数卡/导航角色位；不得变绿。
- 动态文案与数据来自现有状态，不硬编码截图示例。

### 02 运动前检查

参考：`02-运动前检查.png`

- 复刻三步指示、健康说明、原不适入口、运动前记录和底部主按钮；字段与必填规则服从基线，不按图片追加医学字段。
- 异常阻断与必填校验真实生效。

### 03 动作跟练

参考：`03-动作跟练.png`

- 复刻深青绿色沉浸背景、双区示范/我的动作、计时、心率血氧、评分、动作建议和底部操作区。
- 视频/相机/动画必须是真实控件或现有可靠回退，不能静态拼图。

### 04 本次运动报告

参考：`04-本次运动报告.png`

- 复刻评分环、完成结论、训练摘要、前后对比、小喜总结和下一步按钮。
- 所有值来自所选 session。

### 05 康复小报告

参考：`05-康复小报告.png`

- 复刻玻璃心形+绿叶的主视觉、周期和训练指标、四类建议与医生确认边界。
- 禁止奖杯、奖状、金牌和金色庆功风格。

### 06 训练报告中心

参考：`06-训练报告中心.png`

- 复刻单次/阶段/小报告分段入口、趋势区域、报告卡与最近记录。
- 不能牺牲现有单次和阶段详情功能。

### 07 选择使用方式

参考：`07-选择使用方式.png`

- 复刻大面积留白、紫色飞行小喜、日常运动/心脏康复两张入口卡和医疗边界入口。

### 08 关联康复计划

参考：`08-关联康复计划.png`

- 复刻步骤、固定演示医院信息、患者号输入、匹配结果和进入计划按钮；不把展示医院改造成原本不存在的多医院选择服务。
- 医院和患者信息使用 `sharedPatientFixture`，不要照抄图中的占位医院。

### 09 运动选择

参考：`09-运动选择.png`

- 复刻今日运动总览、处方、分类横向入口、训练项目列表和底部导航。
- 保留全部 5 分类和 9 项运动，不得只实现图中两项。

### 10 运动后状态

参考：`10-运动后状态.png`

- 复刻三步指示、运动后数据、Borg 滑块、主观感受、异常入口和生成解读按钮。

### 11 小喜健康助手

参考：`11-小喜健康助手.png`

- 复刻紫色小喜问候、今日计划、最新建议、四个任务快捷入口和底部输入框。

### 12 康复资讯

参考：`12-康复资讯.png`

- 复刻搜索、分类、精选大卡、知识卡、视频卡、健康小队/搭子和内容列表。
- 所有知识内容继续按角色过滤。

### 13 知识文章

参考：`13-知识文章.png`

- 复刻文章头图、内容卡、安全警告与底部返回动作；保留完整原文段落，不硬切为四段。收藏、阅读进度若基线没有，不宣称为已存在能力；新增时另记展示增强并验证。
- 实际正文取自 `knowledgeItems`，且保留医学审核边界。

### 14 知识视频

参考：`14-知识视频.png`

- 复刻视频大卡、标题、待审核/实际时长、安全提示和播放按钮；有实际章节数据才提供章节跳转，不能用装饰章节替代真实播放进度。
- 视频必须可播放。

### 15 训练数据

参考：`15-训练数据.png`

- 复刻顶部三指标、最新建议、身体数据卡、步数及 7 日趋势、最近运动和导航。

### 16 个人中心

参考：`16-个人中心.png`

- 复刻身份卡、小菜园卡、健康管理 2×2 服务网格、原型设置和隐私说明。
- 卡片中的树也不得有土/盆/地台。

### 17 健康档案

参考：`17-健康档案.png`

- 复刻身份/阶段、阶段目标、基础信息、身体指标与来源、医院只读记录和保存按钮。

### 18 设备与数据来源

参考：`18-设备与数据来源.png`

- 复刻设备卡、步数进度、手动录入、优先级说明和原同步动作；授权开关/撤回按5.14边界处理，不虚构系统能力。

### 19 本周运动路径

参考：`19-本周运动路径.png`

- 复刻周进度节点、任务卡、今日建议、下一步说明与开始按钮。

### 20 运动小菜园—成长

参考：`20-运动小菜园-成长.png`

- 复刻中央高质量绿色树体、成长阶段轨迹、今日成长、周期统计和成长说明。
- 树独立悬浮在薄荷白背景；严格禁止土、盆、地面、地台、山丘、岛屿或景观底座。

### 21 运动小菜园—打卡

参考：`21-运动小菜园-打卡.png`

- 复刻自动打卡摘要、连续/累计天数、动态月历、最近记录与规则。

### 22 健康小队

参考：`22-健康小队.png`

- 复刻队伍英雄卡、头像、共同进度、成员状态、提醒按钮、邀请码和隐私规则。

### 23 健康搭子

参考：`23-健康搭子.png`

- 复刻 7/30 天分段、双方关系卡、今日状态、温和提醒和不惩罚说明。

### 24 健康积分与权益

参考：`24-健康积分与权益.png`

- 复刻紫色小喜、积分余额、周任务、里程碑、礼品卡和规则边界。
- 余额和按钮状态必须动态；禁止真实支付。

### 25 训练状态策略

参考：`25-训练状态策略.png`

- 复刻版本、训练前后策略、字段开关、数据来源优先级和演示场景按钮。

### 26 医生审核队列

参考：`26-医生审核队列.png`

- 复刻审核说明、建议卡、时间、变更内容、同意/维持/驳回和审核边界。

### 27 安全停止提示

参考：`27-安全停止提示.png`

- 复刻跟练页面上的暗色遮罩与白色居中模态层。
- 症状项、立即停止、返回训练和紧急说明必须真实可操作。

### 28 训练暂停状态

参考：`28-训练暂停状态.png`

- 复刻冻结的训练内容、紫色小喜休息姿态、暂停说明、继续和结束动作。

### 6.29 逐页运行入口、动作、数据与取证矩阵

下面的矩阵是上述 28 个视觉合同的执行补充。每一页都必须同时满足“能到达、能操作、能变化、能返回、能刷新恢复、能截图取证”六项要求。不得只做一个不可交互的静态页面。若现有实现使用单页状态切换而非路由，继续保留其状态机语义；不要求为了形式统一而强行改成多路由。

#### P01 今日首页

- 运行入口：完成模式选择和必要的患者绑定后进入 `activeNav=today`、`detailView=none`；公众模式和心脏康复模式都要单独验收。
- 关键动作：开始今日核心任务、翻阅患者处方、进入自选运动、进入步数/设备、小菜园、本周路径和报告；任务完成后可直接查看本次报告。
- 数据绑定：问候、日期、用户姓名、处方版本、处方完成数、步数、目标、同步来源、连续打卡、训练分钟和花园成长均来自响应式状态。
- 必测状态：公众未完成/已完成、患者处方未完成/部分完成/全部完成、步数有效/过期/缺失/拒绝授权、无训练记录/有训练记录。
- 视觉取证：分别保存公众与患者首页截图；重点核对绿色层级、主任务唯一性、卡片间距、紫色小喜和五栏导航安全区。

#### P02 运动前检查

- 运行入口：在心脏康复模式、训练前策略不为 `off` 时，从处方或运动项目进入 `detailView=precheck`、`trainingStatus=checking`。
- 关键动作：填写不适评分、选择症状、从设备读取指标、切换手动录入、填写血压/血氧、开始训练；可选策略下允许明确跳过。
- 数据绑定：所有字段写入 `preSnapshot`，来源、采集时间、同步时间和数据质量必须同步更新；不得只改变输入框显示。
- 必测状态：未填写、部分填写、完整有效、设备读取、手动录入、危险症状阻断、策略 required/optional/off。
- 视觉取证：正常可开始和危险阻断各一张；检查三步指示、表单标签、错误提示与底部固定按钮不会遮挡内容。

#### P03 动作跟练

- 运行入口：通过运动前检查或公众模式直接开始后进入 `detailView=training`、`trainingStatus=active`。
- 关键动作：播放/继续训练、暂停、恢复、记录次数、节拍点击、模拟完成、报告不适并停止；具体控件随 `camera-score`、`rep-game`、`rhythm-game` 交互类型变化，video是媒体控件，不是该枚举值。
- 数据绑定：计时、目标时长、进度、动作次数、连续次数、节拍命中、组合数、摄像头状态、实时心率血氧和更新时间必须由训练状态驱动。
- 必测状态：三种交互类型、设备已连接/未连接、摄像头允许/拒绝/不可用、资源加载成功/失败并回退、计时中。
- 视觉取证：至少保存视频跟练、计次互动、节奏互动三张；检查沉浸背景、双画面结构和底部动作在 390×844 内完整可用。

#### P04 本次运动报告

- 运行入口：完成训练及必要的运动后检查后进入 `detailView=session-report`，也可从最近训练记录打开指定 session。
- 关键动作：查看前后指标、规则建议、成长反馈、返回首页/数据页或继续查看报告中心。
- 数据绑定：项目名称、完成状态、时长、得分、前后快照、建议等级、停止原因、积分和花园成长必须来自 `selectedSession`，禁止展示另一个 session 的数据。
- 必测状态：正常完成、提前停止、评估完整、评估不足、稳定/关注/停止/数据不足四种建议。
- 视觉取证：正常完成与停止报告各一张；检查结论措辞不会把规则建议伪装成诊断。

#### P05 康复小报告

- 运行入口：由首页或训练报告中心进入 `detailView=hospital-report`，使用当前用户和当前统计周期数据。
- 关键动作：切换/查看统计周期、阅读四类居家建议、返回报告中心；若存在确认动作，应保持医生确认状态可见但不伪造远程审核。
- 数据绑定：近7/30个本地自然日、completed次数、完成记录去重日期数、有效运动前心率首末值、周期内关联待审核数。仅调用实际存在的数据；没有静息采集条件，不生成静息指标变化。
- 必测状态：数据充足、数据不足、存在停止记录、周期内有/无关联待审核建议；某条运动建议 approved 不等于整份小报告已由医生确认。没有整份报告审核对象时保持待确认边界。
- 视觉取证：保存完整长页截图；确认主视觉为心形健康意象而非奖杯，页面底部必须显示原型/医生确认边界。

#### P06 训练报告中心

- 运行入口：从训练数据页、首页报告入口或个人中心进入 `detailView=training-reports`。
- 关键动作：切换单次/阶段/小报告分段，选择日期、月份或具体 session，打开相应报告详情。
- 数据绑定：单次列表、月度完成率、训练日、平均指标、最低血氧、停止次数和处方完成情况从 sessions 实时聚合。
- 必测状态：空列表、单条记录、多日记录、跨月记录、存在停止记录、公众模式和患者模式。
- 视觉取证：三个分段各一张；验证列表选择态、趋势图、空状态和返回路径。

#### P07 选择使用方式

- 运行入口：清空本地 onboarding 状态或执行“切换使用方式/重置原型”后显示 `onboardingStep=mode`。
- 关键动作：选择日常运动直接进入应用；选择心脏康复进入绑定；打开并关闭健康助手与医疗边界说明。
- 数据绑定：选择结果写入 `mode`，但在流程真正完成前不得错误展示已绑定患者数据。
- 必测状态：首次进入、已有状态恢复、从应用内切换模式、边界弹窗打开/关闭。
- 视觉取证：主页面和边界弹窗各一张；紫色飞行小喜的姿态、留白和两张入口卡必须与效果图一致。

#### P08 关联康复计划

- 运行入口：选择心脏康复后进入 `onboardingStep=binding`。
- 关键动作：查看固定演示医院、输入患者号、执行匹配、错误后重试、返回模式选择、匹配成功后进入康复计划；不新增真实医院搜索/绑定接口。
- 数据绑定：患者号使用现有规范化和 `isSupportedPatientNo` 规则；匹配内容来自 `sharedPatientFixture`。
- 必测状态：idle、loading、matched、error，空输入、错误编号、`256572`、`P-256572`，刷新后的安全恢复。
- 视觉取证：初始、错误、匹配成功三张；验证按钮禁用态、错误文案和隐私说明。

#### P09 运动选择

- 运行入口：从今日页的自选运动区域或分类入口进入 `detailView=exercise-category`，同时保留当前选中分类和返回导航。
- 关键动作：切换五个分类、选择并进入原运动流程；首页自选与患者处方启动继续保留。分类折叠或加入队列不是必须虚构的新能力，按实际处理器核对。
- 数据绑定：分类来自 `exerciseCategories`，项目来自 `exerciseGames`；必须完整呈现 5 类 9 项，推荐项由现有字段决定。
- 必测状态：每个分类、无自选/已选择、项目可开始、患者处方与自选运动的口径隔离。
- 视觉取证：至少覆盖总览与一个展开分类；检查横向滚动、卡片层级、时长/强度标签与底部导航。

#### P10 运动后状态

- 运行入口：心脏康复模式下训练完成或停止且 post policy 不为 `off` 时进入 `detailView=postcheck`。
- 关键动作：同步设备数据、录入/更新心率血氧、选择 Borg、选择主观感受、报告异常、生成报告；可选策略下允许明确跳过。
- 数据绑定：所有值写入 `postSnapshot` 并进入最终 session assessment；不能在生成报告时丢弃用户填写结果。
- 必测状态：required/optional/off、完整/缺失数据、良好/一般/不适感受、正常完成/停止后评估。
- 视觉取证：完整可提交和必填未完成各一张；验证滑块、选择态和底部主按钮。

#### P11 小喜健康助手

- 运行入口：点击五栏导航中间 `assistant`，保持 `detailView=none`。
- 关键动作：打开今日计划、最新建议、档案、设备、报告等快捷任务；文本输入框可输入，并对不可用能力给出明确原型反馈。
- 数据绑定：问候、计划进度、最新建议、用户模式和待办状态来自现有状态；不能伪造云端 AI 医嘱。
- 必测状态：公众/患者、有/无建议、有/无训练记录、输入为空/有内容。
- 视觉取证：两个模式各一张；所有 mascot 均为同一紫色小喜资产族。

#### P12 康复资讯

- 运行入口：点击 `activeNav=discover`。
- 关键动作：搜索或筛选分类、打开精选文章、打开普通文章、播放知识视频；原社交按钮保持暂未开放，另从扩展界面演示验收小队/搭子。
- 数据绑定：内容来自 `knowledgeItems` 并保留 `audience` 过滤逻辑。目前10条均为all，两角色看到同一全集是正确基线；不得声称用这10条已证明患者专属权限隔离。额外隔离测试只能注入可丢弃测试条目，不修改原字典。
- 必测状态：公众/患者、不同分类、无搜索结果、队伍/搭子未加入与已加入。
- 视觉取证：公众和患者内容列表各一张；验证精选大卡、列表图文比例和自然绿色体系。

#### P13 知识文章

- 运行入口：点击 guide/tip 后进入 `detailView=knowledge-article`，`selectedKnowledgeId` 指向所选条目。
- 关键动作：阅读、滚动、返回资讯；若有收藏/已读等原有动作必须继续生效。
- 数据绑定：标题、摘要、正文、受众、审核说明和安全提示来自所选 `knowledgeItems` 条目。
- 必测状态：不同文章、长内容滚动、公众/患者权限、内容不存在时的安全回退。
- 视觉取证：首屏和文末各一张；正文不能用图片替代，字号与行距适合中老年阅读。

#### P14 知识视频

- 运行入口：点击 video 类型内容进入 `detailView=knowledge-video`。
- 关键动作：播放、暂停、拖动实际媒体进度、继续观看、返回资讯；无章节数据不造章节。视频加载失败必须显示可理解回退。
- 数据绑定：标题、时长、章节进度、审核说明和视频源来自当前知识条目或项目资源。
- 必测状态：未播放、播放中、暂停、播放完成、资源失败；H5 和微信小程序分别验收。
- 视觉取证：未播放和播放中各一张；视频大卡中的小喜保持紫色且封面与实际内容一致。

#### P15 训练数据

- 运行入口：点击 `activeNav=data`。
- 关键动作：进入单次报告、报告中心、设备与数据来源，查看 7 日步数趋势和最近运动记录。
- 数据绑定：顶部指标、建议、步数、趋势、最近训练由 metrics、dailySteps、sessions 计算；缺失数据必须显示“未采集/暂无”。
- 必测状态：有/无训练、有/无有效步数、多个数据来源、停止记录。
- 视觉取证：完整数据与空/缺失数据各一张；趋势图不得用写死 SVG 路径冒充动态数据。

#### P16 个人中心

- 运行入口：点击 `activeNav=profile`。
- 关键动作：进入小菜园成长/打卡、健康档案、设备、报告、策略控制台、医生审核、切换模式、重置原型；原积分兑换保持暂未开放，权益页从独立扩展界面演示进入。
- 数据绑定：身份、模式、绑定医院、花园阶段、积分、服务状态与本地状态一致。
- 必测状态：公众/患者、花园不同成长阶段、无/有待审核项、重置确认。
- 视觉取证：两个身份模式各一张；健康管理 2×2 网格需有一致点击反馈，树图禁止出现土或花盆。

#### P17 健康档案

- 运行入口：个人中心进入 `detailView=health-archive`。
- 关键动作：公众模式编辑姓名、出生日期、性别并保存；患者模式查看医院只读信息；返回个人中心。
- 数据绑定：公众资料写入 `publicHealthProfile` 并持久化；患者资料来自共享 fixture，禁止伪造可编辑医院字段。
- 必测状态：公众可编辑/校验/保存/刷新恢复，患者只读，不完整资料和非法日期。
- 视觉取证：公众编辑态和患者只读态各一张；明确区分“用户填写”和“医院同步”。

#### P18 设备与数据来源

- 运行入口：首页步数卡、训练数据或个人中心进入 `detailView=devices`。
- 关键动作：原模拟连接、同步步数、手动录入并保存、查看来源优先级；拒绝/过期/缺失可用可丢弃测试状态验证，不假称已执行系统授权或撤销。
- 数据绑定：`DataSource`、`DataQuality`、syncedAt、goal、steps 和连接状态须真实更新并持久化。
- 必测状态：connected/disconnected、valid/stale/missing/denied、设备数据/系统健康/手动数据、非法步数输入。
- 视觉取证：已连接、拒绝授权、手动录入三张；来源与质量不能只靠颜色区分。

#### P19 本周运动路径

- 运行入口：首页快捷入口进入 `detailView=weekly-path`。
- 关键动作：查看七日节点、选择/定位今日任务、开始训练、返回首页。
- 数据绑定：周节点和完成状态从处方、sessions 与本地日期计算，不能用固定星期完成图。
- 必测状态：周期开始、部分完成、全部完成、今天无处方、跨周。
- 视觉取证：部分完成与全完成各一张；节点连线、今日强调和行动按钮层级与效果图一致。

#### P20 运动小菜园—成长

- 运行入口：首页或个人中心进入 `detailView=garden`、`gardenSection=growth`。
- 关键动作：在成长/打卡分段间切换、查看阶段轨迹、周期详情、训练带来的即时成长反馈。
- 数据绑定：成长日、阶段、收获数、周期起止和今日成长由有效核心训练自动打卡结果计算。
- 必测状态：0/7、成长中、7/7 收获后新周期、有待展示的成长反馈。
- 视觉取证：至少覆盖幼苗、中期、成熟三个阶段；对每张图做主体外轮廓检查，树下必须透明/留白，无土、盆、地台、山丘或岛屿。

#### P21 运动小菜园—打卡

- 运行入口：花园内切换为 `gardenSection=checkin`。
- 关键动作：切换月份、查看动态月历、查看最近记录、返回成长页。
- 数据绑定：连续天数、累计天数、当月记录、来源标签和积分奖励来自 `checkIns`；训练达标自动打卡，同日幂等。
- 必测状态：无记录、单日、连续多日、跨月、同日重复训练、未来月份不可进入。
- 视觉取证：当前月和上一月各一张；日期网格必须由真实年月计算，不能写死 30/31 天排列。

#### P22 健康小队

- 运行入口：“我的→扩展界面演示”进入 `detailView=social-hub`、`socialTab=team`；资讯原按钮保持disabled，不通过开放原入口凑可达率。
- 关键动作：创建演示小队、输入邀请码加入、加入默认演示队伍、提醒成员、切换到搭子。
- 数据绑定：加入状态、邀请码、成员名、成员打卡、共同进度和提醒状态来自 `teamState`。
- 必测状态：未加入、错误邀请码、成功加入、成员未打卡/已打卡、重复提醒。
- 视觉取证：空状态和已加入状态各一张；不得显示成员心率、体重、病史等隐私，也不得做运动量排名。

#### P23 健康搭子

- 运行入口：“我的→扩展界面演示”或小队页切换进入 `detailView=social-hub`、`socialTab=buddy`；资讯原按钮不开放。
- 关键动作：选择 7/30 天周期、体验演示配对、提醒搭子、切换到小队。
- 数据绑定：连接状态、搭子姓名、共同天数、双方今日状态、提醒记录来自 `buddyState`。
- 必测状态：未配对、已配对、7 天、30 天、双方不同打卡组合、重复提醒。
- 视觉取证：未配对和已配对各一张；必须保留“漏打卡不解除、不扣分、不排名”的温和陪伴说明。

#### P24 健康积分与权益

- 运行入口：个人中心独立“扩展界面演示”进入 `detailView=reward-store`；原健康管理积分兑换按钮仍禁用。
- 关键动作：查看积分明细、尝试解锁 MEM、兑换可用权益、处理余额不足或权益不可用。
- 数据绑定：余额、台账、周任务、里程碑、MEM 资格、商品受众、兑换记录由 wallet/ledger/entitlement/redemptions 计算和持久化。
- 必测状态：余额为零/充足/不足、未解锁/可解锁/已解锁、患者专属权益、重复兑换。
- 视觉取证：正常余额、余额不足、MEM 状态各一张；紫色小喜为陪伴角色，不使用奖杯、金牌或真实支付暗示。

#### P25 训练状态策略

- 运行入口：个人中心原型设置进入 `detailView=prototype-policy`。
- 关键动作：切换训练前/后 off/optional/required，开关字段，查看并保留来源优先级，发布新版本，运行四种演示场景；没有原排序控件时不虚构可调整优先级。
- 数据绑定：草稿与已发布策略分离；版本号、发布时间、字段开关和 policy snapshot 必须写入新训练 session。
- 必测状态：未发布修改、发布成功、required 缺字段、off 直达、stable/attention/stop/insufficient 演示。
- 视觉取证：默认策略和修改后策略各一张；策略控件必须具备选中、禁用与说明状态。

#### P26 医生审核队列

- 运行入口：个人中心或报告建议入口进入 `detailView=doctor-reviews`。
- 关键动作：打开待审核建议，执行同意、维持、驳回，查看对应建议和变更内容。
- 数据绑定：审核条目关联 adviceId，状态使用 pending/approved/rejected/maintained，处理时间与结果持久化。
- 必测状态：无待审核、pending、approved/maintained/rejected三类已处理结果、关联建议不存在的容错；共四种状态，不是四种已处理结果。
- 视觉取证：待审核和已处理各一张；任何按钮都只能改变原型本地审核状态，不得宣称已发送真实医生。

#### P27 安全停止提示

- 运行入口：训练进行中点击“不舒服/停止”打开训练页上的安全模态层，不离开当前训练背景。
- 关键动作：选择或确认症状、立即停止、返回训练；紧急危险提示始终可见。
- 数据绑定：停止原因写入 `stoppedReason` 和 post symptoms，停止后正确进入 postcheck 或 session report。
- 必测状态：模态打开、返回继续、确认停止、重复触发、不同症状、训练计时在模态期间的既有规则。
- 视觉取证：保存包含暗色遮罩的 390×844 全屏图；验证焦点/滚动锁定、遮罩层级和危险动作的清晰区分。

#### P28 训练暂停状态

- 运行入口：训练进行中触发暂停后保持 `detailView=training`、`trainingStatus=paused`。
- 关键动作：继续训练、结束训练、打开安全停止（如原逻辑允许）；暂停时训练计时和交互计数不得继续增长。
- 数据绑定：暂停前 elapsed/rep/rhythm/live vitals 保留，恢复后从原状态继续，不重新创建 session。
- 必测状态：短暂停、长暂停、恢复、暂停后结束、视频/计次/节奏三种交互。
- 视觉取证：三种训练交互各至少一张暂停态；检查紫色小喜休息姿态、冻结背景、主次按钮和底部安全区。
- 暂停态原动作必须分别验收：`end-paused-training`调用原`finishTraining(false)`，`demo-complete`调用原`finishTraining(true)`，`training-discomfort`进入独立安全停止模态。前两项按本地原规则生成completed并区分demoCompleted，不得为了符合旧描述而改成stopped；第三项生成stopped且不增加完成积分。不能因为原函数仍在文件里就声称入口语义没有回退。
- 当前休息紫喜资产为`src/static/replica/magpie-resting-v1.png`，内置image_gen生成的不透明深青背景PNG；完整提示词见`docs/REPLICA-PAUSE-ASSET.json`。优先复用已验收资产，不需要每次执行重新生成。按钮、文字与暂停符号为真实组件，不在PNG内烘焙。
- 回归脚本`replica-pause-qa.cjs`在开发服务和独立生产服务分别运行，检查三种交互×三种宽度、暂停/恢复、原普通完成、快速演示、不适取消/停止及对应记录。测试数量读当次输出，不复制旧结论；UI改动必须重建独立产物后再测生产地址。

### 6.30 每一页通用的像素级复刻与功能验收步骤

对 01–28 每一页重复执行下面流程，不得仅抽查：首页做得好不代表其余页面通过。

1. 在 390×844、设备像素比 1 的固定视口打开对应真实运行状态。
2. 保持参考图与实机图相同滚动位置；长页必须补充全页截图，首屏截图不能代替长页验收。
3. 将参考图设为 50% 透明度覆盖在实机截图上，逐项比较页面边界、顶栏、内容起始线、卡片宽高、圆角、内边距、图标中心、字体基线、按钮和底部安全区。
4. 先修结构与尺寸，再修颜色与阴影，最后修文字换行、图标微调和动效；禁止用整体缩放掩盖布局错误。
5. 动态数据导致文字不同是允许的，但同一语义元素的位置、层级、容量和换行策略必须一致。
6. 对每个可点击元素执行一次动作，核对状态变化、返回路径、刷新持久化和控制台；截图相似但动作无效仍为 FAIL。
7. 对 loading、empty、error、disabled、selected、success、warning、blocked、paused、completed 等相关状态逐项取证。
8. 视觉资源必须裁切干净，无生成图片残字、乱码、伪 UI、白边、背景底座和颜色污染。
9. 每页在 `docs/visual-acceptance/latest/` 保存：实机 PNG、对照 PNG、差异说明 Markdown；文件名前缀与 01–28 完全一致。
10. 测试项记录PASS/FAIL/NOT_RUN及实际结果；问题记录OPEN/FIXED/ACCEPTED_FUNCTIONAL_DIFFERENCE；总体使用PASS/REVIEW_COMPLETE_WITH_GAPS/BLOCKED。FIXED必须有修复后证据，不等于整页PASS；功能优先导致的必要差异单独解释。

---

## 七、统一视觉系统：必须全站执行

### 7.1 总体气质

关键词：专业、亲和、轻量、可靠、清爽、可行动、植物感、克制的软 3D、适合中老年阅读。

继续采用“全绿产品界面 + 紫色小喜品牌角色”的组合。小红书案例只借鉴自然绿色层次、植物插画构图和圆角卡片节奏，不照搬橙色/沙漠/花店业务视觉。

### 7.2 建议设计 tokens

先检查现有 CSS 变量并统一收口，避免页面各写一套。建议至少包含：

- 页面背景：`#F5F7F6` / 极浅薄荷渐变。
- 主色深青：`#0F766E`。
- 主色青绿：`#0EA5A4`。
- 品牌亮绿：保留现有 `#22C58B`，仅用于强调和进度。
- 浅薄荷：`#E7FAF3`、`#CCFBF1`。
- 主文字：`#102D2A` 或现有 `#202C28`。
- 次文字：`#64748B` / `#74817D`。
- 分隔线：低对比中性绿灰。
- 危险：使用红色/珊瑚红并同时配图标与文字，绝不使用绿色危险按钮。
- 卡片：白色或极浅薄荷，28–34rpx 主圆角，18–24rpx 子圆角。
- 阴影：低透明度、柔和、不得厚重悬浮。

允许根据效果图微调色值，但必须形成统一 tokens，不能逐页随意偏色。

### 7.3 390 × 844 移动端基线

- 以 390 × 844 CSS 像素作为首要复刻视口。
- 主要横向页边距约 16–20px，卡片间距 12–16px。
- 顶部安全区、微信胶囊区和底部安全区不得被内容遮挡。
- 标题、数字、按钮和正文层级与效果图一致。
- 面向中老年用户：正文不小于可读基线，关键动作建议 16–18px 以上，点击区至少约 44px。
- 长名称、动态数值、系统字体放大时不溢出、不重叠、不截断关键动作。
- 360、390、430px 宽度均做浏览器检查；桌面 H5 使用居中移动端壳或合理响应式，不无限拉宽。

### 7.4 紫色小喜鹊唯一角色规范

所有插画、头像、导航、助手、训练、奖励、资讯和小图标中出现的小喜鹊必须统一：

- 头部、羽冠、翅膀、背部和长尾：紫色/深紫色。
- 高光：淡紫/薰衣草色。
- 面部与腹部：白色或奶白色。
- 喙与脚：橙色。
- 眼睛：黑色大眼，亲和但不幼稚。
- 同一页面不同大小角色需保持同一造型特征。
- 不允许绿色、青绿色或蓝绿色小喜鹊。
- 不要把通用绿色图标误改为紫色；紫色只属于小喜品牌角色及少量配套强调。

优先复用/更新现有：

`src/static/rive-source/v4/master/magpie-neutral-master-v4.png`

以及与紫色角色一致的本地动画、分帧、poster 和 overlay。若旧视频中角色颜色不一致，必须提供可靠的紫色角色视觉方案或明确一致的更新资源，不能在静态页面紫色、训练视频绿色之间来回切换。

### 7.5 图标与插画

- 通用功能图标继续采用圆润线性轮廓、深墨绿+薄荷绿强调。
- 运动、设备、评估、知识、安全图标语义清晰。
- 不使用京东健康红色品牌资产，不复制其商业化组件。
- 不使用奖杯、金牌、皇冠、金色奖状或疗效庆祝。
- 医院/安全可用盾牌、记录、心电、设备等中性图形。

### 7.6 动效

- 动效服务于状态理解：进度、打卡、暂停、完成、树成长、小喜轻动作。
- 尊重 reduced motion；减少动效时仍能理解状态。
- 不使用持续闪烁、夸张粒子或让中老年用户分心的动效。
- 不要为了酷炫牺牲小程序性能。

---

## 八、实现约束

1. 保持 UniApp + Vue 3 + TypeScript 技术栈。
2. 不要把 28 张整页效果图作为 `<image>` 背景覆盖页面。
3. UI 必须由真实 `view/text/button/input/switch/video/swiper/scroll-view` 等组件构成。
4. 优先复用现有组件与状态模型；若拆分 `index.vue`，必须保持全部函数行为和持久化兼容。
5. 不引入重型 UI 框架去覆盖现有设计系统。
6. 静态资源使用项目内相对路径；H5、微信小程序和 App 构建都能解析。
7. 不依赖运行时外链图片、字体或 CDN 才能显示核心页面。
8. 保留所有已有 `data-testid`；新增页面/关键动作也要有稳定、语义明确、静态可枚举的测试锚点。
9. 交互动作必须有：当前允许状态、守卫、执行反馈、持久可见结果、失败恢复和可测试结果。
10. 核心动作不能只显示 Toast；状态变化必须在卡片、列表、报告或详情中持续可见。
11. 防止嵌套按钮、父级点击吞掉输入/开关/子按钮事件。
12. 不使用全局无约束 `.active/.disabled/.error/.success` 污染其他组件。
13. 不通过大量 `!important` 强行覆盖。
14. 不修改真实 GitHub remote，不自动 commit/push/deploy。

---

## 九、必须覆盖的界面状态

对适用页面实现并测试：

- default：正常数据。
- empty：说明为什么为空，并提供下一步。
- loading：显示加载范围，禁止重复提交。
- error：显示原因、重试或安全回退，保留用户输入。
- no_permission：说明权限边界，不泄露隐藏数据。
- partial/stale：说明缺失或过期，并限制有后果动作。
- success：结果持续可见，并给下一步。

重点状态样例：

- 患者号匹配成功/失败。
- 设备未连接/授权/同步中/同步成功/拒绝/数据过期。
- 训练前必填不完整/危险症状阻断/可开始。
- 训练 active/paused/stopped/completed。
- 训练后完整/部分/缺失。
- 建议 stable/attention/stop/insufficient。
- 医生审核 pending/approved/rejected/maintained。
- 报告空列表/单次/阶段不足/阶段可生成。
- 小队未加入/申请中/已加入/已提醒。
- 搭子未连接/7天/30天/已提醒。
- 积分不足/可兑换/兑换成功。
- 小菜园未成长/今日已成长/第7天收获/月历切换。

---

## 十、医疗、安全、隐私与原型边界

全站必须遵守：

- 不诊断疾病。
- 不调整药物。
- 不自动修改医院处方。
- 不把单次心率、血氧、Borg 或动作评分解释为疗效。
- 不把训练完成、打卡、树成长、积分或徽章描述为康复效果证明。
- 危险症状应立即停止并建议根据医院指引联系专业人员；紧急情况提示呼叫 120。
- 缺失/过期数据不显示为 0、正常或绿色安全状态。
- 所有健康数据、处方、设备、支付、兑换、消息、组队与提醒都是本地原型。
- 不读取真实健康数据，不发送真实消息，不执行真实支付或物流。
- 社交不共享健康档案、不按身体数据排名。
- 医生审核页明确是本地工作台演示，最终处方由医生确认。

---

## 十一、一次跑完的实施顺序

按照下面顺序连续执行，不要每一步停下来等确认：

### 阶段 A：冻结基线

1. 检查目标目录与原目录 Git 状态和提交。
2. 执行并记录现有 `npm run type-check`、`npm run test:rive-runtime`、`npm run build:h5`。
3. 启动现有 H5，使用 390 × 844 视口保存“改造前”关键截图。
4. 生成 Stage 0 交互与页面盘点。

### 阶段 B：建立统一设计系统

1. 抽取颜色、字号、圆角、间距、阴影、按钮、状态和安全区 tokens。
2. 统一顶部栏、底部导航、卡片、主/次/危险按钮、分段控件、指标卡、列表、空状态和模态层。
3. 建立紫色小喜唯一资产规范。
4. 建立树体唯一视觉组件/资源，严格无土、盆和地台。

### 阶段 C：按闭环实现，不按孤立截图实现

1. 启动与双模式。
2. 今日首页与运动选择。
3. 训练前 → 跟练 → 暂停/安全停止 → 训练后 → 本次报告。
4. 训练报告中心与康复小报告。
5. 训练数据、健康档案、设备来源。
6. 小喜助手、康复资讯、文章与视频。
7. 本周路径、小菜园成长/打卡。
8. 健康小队、健康搭子、积分与权益。
9. 训练状态策略与医生审核。
10. 全局响应式、可访问性、动效和资源收口。

每完成一个闭环就立即运行页面并修复，不要把所有问题留到最后。

### 阶段 D：功能等价性回归

对比原项目与新副本：

- 路由/视图数量。
- `data-testid` 数量与归属。
- 所有动作处理函数。
- 类型枚举与状态转换。
- 表单字段与验证。
- 本地存储字段与迁移。
- 角色路径与数据范围。
- 运动项目与资源。
- 报告、积分、打卡、小队、搭子、设备和审核结果。

任何“视觉更干净但行为消失”的变化都必须恢复。

### 阶段 E：真实浏览器视觉与交互验收

1. 以 390 × 844 打开 H5。
2. 逐页完成 28 个效果图状态的真实截图，命名为 `01`—`28` 对应文件名。
3. 与效果图并排比较：整体布局、区域高度、间距、圆角、字体层级、色彩、插画比例、底部按钮和安全区。
4. 不得以肉眼“差不多”作为结论；记录每页差异并修复显著偏差。
5. 再检查 360 × 800、390 × 844、430 × 932。
6. 检查页面滚动、固定底栏、键盘弹起、长文本、动态数据、空状态和模态层。
7. 检查浏览器控制台错误、资源 404、视频播放错误和重复计时器。

验收证据保存到：

`docs/visual-acceptance/latest/`

至少包含：

- 28 张运行截图。
- `visual-diff-report.md`。
- `interaction-run.md`。
- `console-errors.txt`，若无错误明确写 `0 errors`。
- 关键构建输出摘要。

### 阶段 F：最终构建与回归

必须依次运行并修复到通过：

```bash
npm run type-check
npm run test:rive-runtime
npm run build:h5
npm run build:mp-weixin
npm run build:app
```

若某个平台因当前机器缺少外部签名/登录无法完成正式打包，至少完成不需要账号的本地编译，并准确记录限制；不能把账号缺失误报为代码失败。

最后重新启动 H5，做一次核心患者流程和公众流程冒烟测试。

---

## 十二、必须执行的端到端验收场景

### ARUN-01 公众用户完整流程

选择日常运动 → 进入首页 → 浏览5分类 → 选择运动 → 公众前后检查按原关闭策略处理 → 开始训练 → 暂停 → 继续 → 完成后直接报告 → 查看数据 → 刷新记录仍存在。不得强行要求公众填写患者运动后表单。

### ARUN-02 患者计划完整流程

选择心脏康复 → 输入合法患者号 → 匹配处方 → 今日处方轮播 → 开始处方运动 → 完成训练前必填 → 跟练 → 训练后必填 → 生成建议/报告 → 处方完成数更新 → 自动打卡/积分按规则更新 → 刷新后保持。

### ARUN-03 非法患者号

输入错误患者号 → 显示明确错误 → 保留输入 → 可返回/重试 → 不进入患者首页；再分别验证256572与P-256572。

### ARUN-04 危险症状阻断

训练前触发原不适入口 → 不能开始训练 → 显示停止与联系医护说明 → 保留stopped原因记录，但不产生completed记录、完成积分或自动打卡。不要为了“阻断”删掉原停止记录。

### ARUN-05 训练中暂停

训练中暂停 → 计时不增长 → 页面与 28 号图一致 → 继续恢复 → 不创建第二计时器。

### ARUN-06 训练中不适停止

训练中点击“我有不适” → 27 号模态层 → 选择症状 → 状态变 stopped → 保存原因 → 不打卡、不发积分 → 报告中保留停止记录。

### ARUN-07 设备与缺失数据

模拟未连接 → 原来源同步 → 手动补录守卫 → 再次同步优先 → 用可丢弃状态验证拒绝/过期/缺失 → 不把缺失当0或正常。系统授权/撤回没有实机证据时标NOT_RUN，不冒充原功能已验收。

### ARUN-08 报告闭环

完成至少 2 次不同运动 → 单次列表与详情正确 → 阶段报告按有效天数门槛显示 → 小报告从真实记录聚合 → 所有来源和边界可见。

### ARUN-09 小菜园与打卡

完成当天第一项有效核心运动 → 自动打卡 +5 → 树成长一次 → 同日第二次运动不重复成长/奖励 → 月历更新 → 刷新保持 → 停止训练不成长。

### ARUN-10 小队与搭子

先验证资讯原小队/搭子入口仍disabled，再从扩展界面演示创建/加入小队 → 成员状态 → 单日提醒 → 刷新保持；切换搭子7/30天 → 配对 → 提醒 → 漏打卡不解除、不扣分；页面无身体数据排名。

### ARUN-11 积分与权益

余额不足按钮禁用 → 余额足够兑换 → 余额减少 → 兑换记录出现 → 刷新保持 → 不出现人民币支付或充值。

### ARUN-12 医生审核

生成attention/stop建议 → 关联审核队列pending → 分别验证同意、维持、驳回 → 状态持久；同意只按源语义更新planAdjustment，前后核对共享处方fixture未改，不把本地点击当成真实医生签署。

### ARUN-13 知识与视频

资讯页按角色过滤 → 打开文章 → 完整正文/边界 → 返回 → 打开视频 → 播放本地资源 → 无 404/黑屏 → 返回状态正常。

### ARUN-14 持久化与迁移

构造当前schemaVersion 7、旧v3、旧points/checkInDate/streak、空数据与损坏数据 → v7刷新保持 → v3按源规则载入并存为7 → 验证旧字段的实际回退结果并明确哪些并非无损迁移 → 损坏结构修复前保留原始备份 → 页面不白屏。不能把6条演示打卡回退描述为真实旧打卡恢复。

---

## 十三、逐项完成门禁

只有以下全部满足才可以宣称完成：

- [ ] 28 张效果图全部有真实可达页面/状态。
- [ ] 28 张运行截图全部生成并逐页核对。
- [ ] 所有小喜鹊均为紫色，没有绿色角色残留。
- [ ] 小菜园树无土、盆、地面、地台、山丘、岛屿或橙色背景。
- [ ] 没有奖杯、奖状、金牌和金色疗效庆典。
- [ ] 日常运动与心脏康复双路径都可完成。
- [ ] 5 个分类和 9 项运动全部保留。
- [ ] 训练前/中/后/暂停/停止/完成状态全部可用。
- [ ] 异常症状会真实阻断或停止训练。
- [ ] stopped安全停止不新增打卡或完成积分；快速演示/活动中提前结束及MEM挑战分别严格遵循源奖励规则。
- [ ] 单次、阶段、小报告完整。
- [ ] 训练数据、步数趋势和来源完整。
- [ ] 助手四类任务与自由输入完整。
- [ ] 文章、视频和医学边界完整。
- [ ] 健康档案、模拟设备同步、手动步数和来源质量完整；不把源中不存在的系统授权撤回能力伪称已实现。
- [ ] 本周路径、小菜园成长和月历完整。
- [ ] 小队、搭子、温和提醒和隐私规则完整。
- [ ] 积分、里程碑、兑换、MEM 规则完整。
- [ ] 策略发布和医生审核状态完整。
- [ ] 当前schemaVersion 7刷新保持，旧v3兼容及旧字段实际回退有证据；无损迁移未实现的部分明确记录。
- [ ] H5、微信小程序、App 本地编译通过。
- [ ] TypeScript 检查通过。
- [ ] Rive 运行时测试通过。
- [ ] 核心本地资源无 404。
- [ ] 浏览器控制台 0 error。
- [ ] 360/390/430 宽度无关键溢出。
- [ ] 所有已启用按钮、输入、开关、页签、返回和模态层按合同可操作；原禁用入口仍禁用，不能为了“全部可点击”取消限制。
- [ ] 原有 `data-testid` 未丢失，新增关键动作有稳定锚点。
- [ ] 没有用整页截图伪造界面。
- [ ] 没有修改源项目、远程仓库或线上环境。

任一项未通过时，继续修复并重跑相关回归，不要提前结束。

---

## 十四、最终交付格式

完成范围内工作后给一份明确、证据化的最终报告；确有未完成事项就列明，不以措辞要求掩盖。报告必须包含：

1. 结果：是否完整复刻、是否功能无回退。
2. 实施目录。
3. Git 起始提交和最终工作区变更摘要。
4. 28 张页面对应的源文件/组件和验收截图路径。
5. 紫色小喜统一检查结果。
6. 小菜园无土/盆/地台检查结果。
7. 功能等价性对照：保留、重构、新增但不改变规则的内容。
8. 14 个 ARUN 场景逐项 PASS/FAIL 和证据路径。
9. `type-check`、Rive 测试、H5、微信小程序、App 构建结果。
10. 浏览器尺寸、访问地址、控制台错误数量。
11. 真实限制和未完成项分开列：代码/视觉缺陷、未执行验证、外部账号/证书/设备条件、包体与发布条件分别说明。不能把未完成实现包装成外部限制，也不能为了交付格式隐瞒它。

使用下面的完成锁：

```text
[PROTOTYPE LOCK]
truth_version=c1b56ff43a94c0e7e096bb80f0f19f998673a48c
target_project=/Users/guagua/Documents/冠心病课题/小喜鹊精简功能版-原型副本
visual_reference_count=28
view_count=<实际值>
action_count=<实际值>
state_count=<实际值>
role_paths=public,cardiac
browser_evidence_status=pending|passed|blocked
aesthetic_decision_ref=全绿界面+紫色小喜+独立树体
gaps=<无则写 none>
evidence_location=docs/visual-acceptance/latest
```

现在开始执行：先阅读配套《复刻执行细则与证据清单》，恢复已有Stage 0和当前实施证据，再继续实现、运行、逐页验收、修复和构建。不要重复覆盖已完成实现；普通问题自行处理，超范围事项隔离记录。不得用“完美”“一次跑完”等目标性措辞替代可核验结果。
