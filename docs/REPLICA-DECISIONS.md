# 复刻实施裁决与功能真相

本文件补充 28 页清单与主提示词。权威规则：本地功能基线优先，效果图约束视觉；旧提示词对源项目的推测不自动成为新业务规则。

## 已核实的冲突（必须保留在后续提示词中）

| 编号 | 原材料或效果图表述 | 本地事实与实施裁决 |
|---|---|---|
| DEC-006 | 当前存储为 schemaVersion 3 | 实际保存版本为 7；v3 能载入，旧版步数按源项目重建演示数据。不能把 v3 称为当前版本。 |
| DEC-007 | 快速演示或提前结束不发放任何积分 | 源 `finishTraining` 将这两种路径标为 completed，随后执行自动打卡与既有奖励。完整保留该语义；MEM 完整八式奖励另有更严格条件。停止路径仍不发积分。不得把演示训练验收说成真实全时长训练验收。 |
| DEC-008 | 旧 checkInDate、streak 完整迁移 | 源项目对空/缺失 checkIns 回落 6 条演示打卡，并非完整恢复旧字段。保留其现状并明确说明。 |
| DEC-009 | 资讯中小队/搭子、我的积分均可直接开放 | 原入口确为 disabled/暂未开放。保留原状态；在“我的→扩展界面演示”提供独立本地预览，不宣称业务上线。 |
| DEC-010 | 单次训练报告是一条 session | 原报告中心按自然日合并当天运动；单条 session 有独立本次运动报告。两个层次同时保留。 |
| DEC-011 | 小报告展示静息心率首末改善 | 训练快照只证明运动前采集值，不保证静息条件。新增小报告标注“运动前心率”，且明确不等同静息趋势、不判断疗效。 |
| DEC-012 | 患者首页处方八段锦10/20分钟；步数8000 | 使用共享患者 fixture、实际处方项目/时长和 DAILY_STEP_GOAL=6000，不抄图中示例。 |
| DEC-013 | 助手已存在四任务工作台 | 源组件是3个问题与自由输入；本次新增四类结构化任务回答和真实快捷跳转，保留原问答行为。 |
| DEC-014 | 设备页已具备全部授权开关/撤销操作 | 源设备页是模拟来源同步和手动步数，未实现完整授权撤回产品流程；本次保留实际入口，不虚构系统授权已发生。缺失/过期/拒绝状态通过本地故障数据验证。 |

## 本次安全修复与结构增强

- FIX-001：H5 摄像头已经显示画面，但 renderjs 回传未更新 Vue 状态；改用 H5 组件内媒体运行逻辑，App/微信各自路径保留。浏览器验证使用 Chrome 合成摄像头，没有访问用户真实摄像头。
- FIX-002：示范 MP4 与摄像头画面同步响应暂停/继续；互动计数仍沿用 active 守卫。Rive 加载失败显式切换已有视频回退。
- FIX-003：存储数组含 null/非记录对象时导致恢复中断；新增结构校验，不改有效数据语义。首次修复前把原始内容备份到 `magpie-prototype-state-recovery`，没有删除用户文件。
- UI-001：新增 OnboardingView、RehabMiniReport、KnowledgeDetail 组件；原有稳定测试锚点继续保留。
- UI-002：新增搜索、分类、任务型助手、报告概览、周期切换、小报告建议展开、独立演示入口、MEM 解锁入口及邀请码复制；都使用本地现有对象与状态。
- UI-003：抽取统一绿色 tokens，保留紫色角色；花园从白菜图形换为独立树体，不含土、盆或地台。树成长不代表康复疗效。
- UI-004：首页处方提取为 PrescriptionTaskCard，保留4项处方、轮播、完成状态、AR标记、原海报和启动守卫；医生确认说明扩展卡片高度。SessionReportSummary 按实际 poseScored/score 显示分数或“已记录”，停止单独标记，不填效果图88分；前后指标逐格读有效快照，保留未采集、来源、单位和解释。
- UI-005：助手改为独立内容滚动区与常驻输入栏，提交后定位到新问题；4任务、4快捷入口和安全优先问答保持。手机软键盘与原生读屏未验收，不能把浏览器常驻栏测试等同真机证明。
- UI-006：权益页用5个独立生成的本地产品插图替换文字占位，紫色小喜盾牌不含奖杯/金牌；保持原礼品ID、顺序、角色过滤、价格、币种、可兑换守卫及确认/取消/扣分/记录。MEM演示区移到礼品与兑换记录之后，入口未删除。仅显示层重排，不改变业务开放状态。

## 只读边界与验收措辞

- UI-007：ExerciseSelection独立呈现今日完成项目/记录、连续打卡、有效步数、患者4项处方入口和五类九项运动；处方卡明确行高，避免默认按钮行高撑高。只传递原选择/处方入口事件；不填图中的热量、平均心率或示例时长。三尺寸字号至少11px断言仅覆盖本组件，不扩大为全站无障碍通过。
- UI-008：KnowledgeHub改为精选大卡、三张重点卡、康复圈、剩余文章与视频；全部10个原ID各展示一次，筛选/搜索不丢内容。原10条audience均为all，保留过滤规则但不捏造患者专属测试成功。CompanionHub的entryOnly仅调整行式布局和外边距，disabled守卫不变；小队/搭子详情保持。
- UI-009：资讯新插图采用浅薄荷不透明背景，去除multiply混合，避免背景叠乘形成深色矩形；并非声称已提取透明图。逐个检查原3视频实际播放、4张新增装饰实际加载和两页文字字号，证据为catalog-run.json。
- UI-010：启动双卡采用新生成绿色鞋与复用检查表PNG；保持紫色小喜和两模式事件不变。叶片装饰新增独立裁切层，消除装饰撑宽而非关闭溢出断言。打卡摘要用growthArtwork共享树体、连续/累计两指标和实际已打卡/待打卡状态；checkIns、自然月和成长规则未变。

- UI-011：KnowledgeDetail文章采用检查表封面和图文横向章节，7篇文章仅添加显示标题，原body字符串、顺序、段落数及返回事件不变；视频仍使用实际播放进度。参考中的4章、医生已审、收藏和42%阅读进度不作为新功能伪造。封面插画改为卡内定位，解决360宽度撑出边界。
- UI-012：个人中心调整为左文右树、横向图标管理卡，保留真实身份、成长周期、连续/累计、档案/设备/报告及原积分禁用入口。原型设置、切换/重置和独立扩展预览全部保留，故页面仍可滚动而非压成一屏；不照抄参考的“加密存储”承诺。
- UI-013：审核卡增加图标、建议区域、三枚描边按钮、已处理状态/时间与本地边界说明。resolveReview函数未改，approved仍只写planAdjustment而不改患者fixture；maintained/rejected不覆盖已有说明。DetailIntro增加可选review外观，未改变其他页面的标题样式。可丢弃长文/三种处理/空队列测试验证实际字段与刷新结果；不抄图中的瓦数、Borg阈值或预计疗效。

- UI-014：02/10增加同系薄荷提示卡，前检查不适滑杆/危险入口/3个血压血氧字段和原必填规则保持；后检查按原策略显示心率/血氧/Borg/感受并进入原报告快照，不照抄参考新增症状表单。字段/感受/同步按钮扩大，三宽度检查文字≥11px及输入/按钮实际尺寸；不据此声称全站无障碍或临床阈值验证。
- UI-015：15训练数据改为图标统计、紫喜建议、身体数据分组、左步数右7日图和原近7次记录。totalMinutes/totalCompletedSessions只统计completed、停止不计入；原2个展示指标和角色fixture不改，不增设BMI假值。步数进度绑定原todayStepProgress，目标6000；缺失/过期/拒绝不展示无效步数。默认7日稠密数值三尺寸不重叠；原归一化会补localDate/poseScored/fullRoutineCompleted，验收比较归一化后的记录，不能误判为展示层修改。
- FIX-004：快速完成离开训练时曾出现未处理play请求取消。只读确认Uni H5的VideoContext内部video.play没有返回/处理Promise；MagpieMotion的H5分支改为持有原生play Promise，组件离开/AbortError按取消处理，其他错误继续发出error事件。微信/App仍使用原VideoContext。连续12次快速切换复核，无访问真实摄像头；测试覆盖取消路径，不冒称所有媒体故障均处理。

原项目与效果图目录只读；未推送、未提交、未部署、未连接真实医院/支付/消息。源字典、患者 fixture、Rive 资源映射、pages/manifest/package 核心文件未改。

“通过”必须带验证范围：静态函数/资源保留不等于每一个交互已执行；Chrome H5 通过不等于微信/App 真机通过；三端编译不等于商店可发布包；运行截图不等于像素完全一致。

证据目录：`docs/visual-acceptance/latest/`。尚未完成的视觉复核或外部设备测试应如实列为缺口，不以“完美复刻”替代证据。

## 全页可读性、点击区域与资源审计补充

- UI-016 / REV-A11Y-001：延续全绿界面与紫喜角色，统一低于23rpx的显式小字号；对已测浅色背景说明文字提高前景对比度。白字按钮、步骤数字和已完成徽标采用更深绿色，播放覆盖图标改为实色底，避免用透明覆盖假设图片对比度。不得把纯色CSS计算推广为渐变、栅格图片或读屏验收。
- UI-017 / REV-TOUCH-001：按钮与输入框默认至少44px；原较小的按钮min-height改为92rpx。页头两侧圆按钮改92rpx、页头124rpx，同步调整移动/桌面滚动区扣减高度。输入组件与开关/滑条在H5扩大实际事件命中区域；不更换原handler，不新增阈值、字段或发布行为。策略开关上下边缘点击切换后恢复，未发布草稿，已发布字段不变。
- FIX-005：放大控件后原396rpx处方轮播出现部分卡片按钮超出卡底；普通/带说明高度调整为440/524rpx，三尺寸全部4处方与长说明重新检查。不能通过隐藏溢出或删文案让测试过关。
- FIX-006：Uni H5组件默认display:block覆盖低优先级全局样式，导致放大后的页头返回与策略页签文字靠上。H5使用uni-page-body uni-button限定默认flex，组件已有grid/flex/布局规则继续优先；text-button显式flex。新增实际文字中心与按钮中心偏差检查，不仅断言CSS属性。
- REV-A11Y-002（部分修复，实施Agent负责）：H5已补充按钮/开关语义、Enter/Space激活、评分方向键和Home/End、处方轮播及隐藏卡隔离、弹窗安全默认焦点/Tab约束/Escape/回焦点/背景锁。keyboard-run.json覆盖8项本地场景；完整读屏、所有焦点顺序组合和微信/App原生键盘仍未证明，不能关闭整个无障碍评审。
- REV-PKG-001（部分实施，实施Agent负责）：独立分发白名单现由源文件+编译引用并集、完整动态前缀及选中资源本地依赖闭包生成，61项静态资源保留，171项仅不纳入分发；所有原文件/视频/紫喜母版保留。三端代码与配置原样复制，hash及H5文件HTTP/视频Range逐项核验；最终浏览器结果见runtime-delivery目录。平台分包/可上传包及真机仍未证明，未上传素材、未替换外链、未修改源字典、manifest或pages配置。
- REV-A11Y-003（开放，实施Agent负责）：渐变/图片/透明度测量仍需逐区域像素检查；44px DOM尺寸和鼠标边缘命中不等同微信/App真机触控。全条件状态、软键盘和原生读屏没有凭空填写PASS。

新增证据：`readability-run.json`（28页×3尺寸）、`accessibility-run.json`（4项有界专项）、`resource-audit.json/md`（资源台账）。字号调整后、触控/配色调整前的诊断保存在`visual-acceptance/readability-before-controls-20260907.json`；此快照不是字号修改前的原始基线。

## H5键盘补充与影响范围

- UI-018：`prototype-keyboard.ts`仅在H5挂载/卸载，调用现有click与处方切换处理器，不保存患者、训练、策略、积分或兑换状态。原生输入/视频保留平台键盘行为；原disabled入口继续不可激活。按钮、输入焦点有可见轮廓，不以图片替代交互。
- UI-019：`H5KeyboardDate.vue`仅在H5渲染原生date输入，键盘聚焦时可见；原Uni日期选择器保留指针入口。两者发送同一change载荷到原setPublicBirthDate，仍由原保存逻辑校验未来日期及年龄。测试使用fill和Tab，不冒称所有日期分段物理键组合均已验证。
- FIX-007：键盘Enter打开原生兑换弹窗时，框架document keyup可能把同一次松键当成新弹窗确认。适配器仅消耗已处理的Enter/Escape对应keyup，抑制长按重复激活。回归分别验证打开无扣分/无新记录、Escape取消、显式确认500→450且仅新增一条本地兑换；没有修改商品价格或原兑换函数。
- 直接影响：P01处方、P02/P10评分、P07边界弹窗、P17公众日期、P24确认弹窗、P25字段开关、P27停止弹窗；所有H5自定义按钮为回归影响。医疗字典、共享处方、奖励和审核规则无业务变更，6核心文件仍需每轮hash复核。
- 验收责任：自动化检查由实施Agent执行，不冒充产品、医学、用户视觉或平台签署。仍保持REVIEW_COMPLETE_WITH_GAPS。

## 独立资源分发与生产运行补充

- BUILD-001：`replica-runtime-delivery.cjs`只创建新命名的dist/runtime-delivery/run-*，不覆盖旧分发/源项目/原构建。选中素材逐字节复制；所有非static产物（JS、CSS、页面、配置、原生组件及库）保留。使用已通过且不早于源码修改时间的三端构建。来源/排除依据、文件hash、构建时间和目录见runtime-delivery.json。
- BUILD-002：`replica-delivery-qa.cjs`把HTTP实际返回与分发hash比对，不能只看200；Vite对不存在素材可能返回首页HTML，测试明确确认它是首页而非原历史图片。6视频的Range与原字节比较，内容/时长没有转码或裁剪。
- QA-001：生产H5没有可调用的全局uni.getStorageSync。测试改为读取当前安装框架实际写入的`{type,data}`封装；播种也写相同封装到可丢弃浏览器上下文，下一次刷新由原恢复逻辑处理。没有给应用注入业务函数/更改状态守卫。默认测试仍访问4173，分发版访问4183并输出独立证据目录。
- FIX-008：生产框架的屏幕外`body::after`会在3秒后预加载`https://cdn.dcloud.net.cn/img/shadow-grey.png`，曾出现ERR_CONNECTION_CLOSED。H5增加`html body::after { animation:none; background-image:none; }`关闭无业务意义的远程装饰预加载；不删除组件、不屏蔽错误日志、不改网络API。浏览器测试记录外部请求与失败URL，任何控制台错误/HTTP失败/非取消网络失败/外部请求都会使任务返回失败。
- 影响与证据：BUILD-001涉及三端分发选择，所有28页/9运动/10知识为资源回归范围；FIX-008仅H5框架装饰，原五栏/训练/报告/积分规则无变动。构建与默认回归重新执行；精简版真实播放、目录、键盘和28页截图另存`visual-acceptance/runtime-delivery/`。大小下降不是发布或用户签收。
- FIX-009：独立生产H5的可读性扫描发现P05未选中周期标签前景#57766c、背景#e3f3ed，纯色对比度约4.35，三个尺寸重复出现；开发版扫描未暴露此组合。仅将组件本身未选中前景改为#48685d，不改布局、周期切换或聚合。需要以重新编译后的生产截图/测量关闭该项，不能只引用开发版0项结果。

## 暂停页视觉细化

- UI-020：暂停标题显示“训练已暂停”，保留所选真实运动名称/计时；使用内置image_gen新生成休息紫喜与CSS暂停符号。继续为全宽主按钮，普通结束与原不适入口并列；快速演示仍可访问，不能为凑一屏隐藏原功能。
- UI-021：参考28未展示原不适与快速演示入口，但原功能优先保留；支持实时指标的项目仍按原条件显示，太极不填参考中的虚构心率。说明明确如有不适应选择“我有不适”，避免引导到普通完成按钮；没有新增医疗诊断或自动完成。
- FIX-010：对照原项目989—1002行发现暂停态普通结束被此前复刻映射成停止，且隐藏快速演示，与DEC-007及功能优先冲突。恢复暂停“结束本次运动”调用原finishTraining(false)，演示调用原finishTraining(true)，二者仍按源规则生成completed；“我有不适”单独打开安全停止模态并生成stopped。这里只恢复原入口/调用，不改函数或奖励语义；不能把这种原型规则宣传为医疗有效训练判断。
- QA-002：新增pause-run.json，三类训练各覆盖三种宽度、图像解码、按钮尺寸/顺序、计时冻结恢复、暂停互动禁用、不适取消/停止及停止不增加积分打卡；每类另测暂停后普通完成和演示完成、记录唯一性与demoCompleted区分。使用可丢弃浏览器状态与合成摄像头，不访问真实健康资料；开发版和独立生产版分别取证。
- 资产与分发：本轮新增一张不透明PNG，运行素材白名单由61增加为62，历史171项仍仅不纳入独立分发，原项目文件没有删除。首版棋盘格输出弃用但保留追溯，提示词见REPLICA-PAUSE-ASSET.json。
- QA-003：首次分发文件测试漏设4183地址而误测默认开发服务，准确报出hash/历史素材不符；诊断保留在diagnostic-history/delivery-wrong-dev-url-20260907.json，重新显式指定4183。此失败不是应用资源丢失，也不能把开发服务结果当分发验证。

## 安全停止页面合同与验证

- UI-022 / P27：复用原stop-modal、symptomOptions前三项、stopTraining(item.label)、主动停止和返回处理器；不加新症状、不增加选择后第二次确认。新增静态stop-symptom锚点和data-symptom，保持原中文标签（不抄参考额外“乏力”字段）。居中标题、左图右文症状卡、白色圆角容器与3枚内置image_gen局部图标；患者/公众共享视觉，角色/策略分支不改。
- FIX-011 / REV-A11Y-004：原危险主按钮亮红两端与白字对比约2.64/3.69，本轮只加深该渐变为#d03d36/#c52f37。实际CSS提取确认白字和两色stop，使用逐通道最大值为sRGB插值建立保守上界，并检查11个插值样本，对比度下界约4.77；该有界证明不覆盖其他图像/渐变或读屏。首次测试误要求第二端所有通道都更暗，但蓝通道高1；已改为有效的保守上界检验，未改弱4.5阈值。诊断见diagnostic-history/safety-gradient-guard-20260907.json。
- QA-004：safety-run四组公众/患者×active/paused；每组3宽度、五按钮目标≥44px、标题居中、图像解码、返回不增记录；原3症状+主动停止共16个记录分支，逐一核对原因、stopped、钱包/打卡不增加、刷新保持。专项患者pre/post off用于隔离记录，required后检查仍由主browser/keyboard回归，不冒称穷尽所有策略组合。
- 非变更：弹窗打开本身不自动改变训练状态，原active/paused语义保留；取消仍回到相同状态。没有修改原停止/完成/奖励函数、医疗阈值或真实医院处方。
- 素材：新增3图后白名单65项，未纳入分发仍171项；当前数值以runtime-delivery.json为准。原文件未删除；分发仍不等于小程序可上传包。引用提示词和来源见REPLICA-SAFETY-ASSETS.json。
