# 续接检查点：13 / 16 / 26细化

目标仍为全部28页功能保真与视觉升级，未完成，保持REVIEW_COMPLETE_WITH_GAPS。不能把本轮3页通过当作像素一致、完整交付门禁或平台发布验收。

## 本轮修改

- KnowledgeDetail：检查表封面、语义分节标题、左图右文；7篇原body字符串/顺序/数量保持，视频播放逻辑不变。新增knowledge-paragraph锚点。修复360宽度封面插画撑出边界。
- index个人中心：左文右树、2×2横向图标管理卡，身份/计数/成长树仍真实动态；原健康档案、设备、报告、打卡、设置、重置、模式切换和扩展入口保留，积分兑换仍禁用。所测区域辅助文字≥11px。
- index审核：图标、建议内容、三描边操作、已处理状态/时间和本地演示边界；resolveReview函数原样保留，只有approved更新planAdjustment，不更改共享患者fixture。
- DetailIntro：可选appearance=review用于审核标题/提示条。Uni编译会隔离页面样式，原父页面选择器未生效，改为组件自身可选样式并加入浏览器实际padding断言；其他标题未改。
- 更新REPLICA-DECISIONS、visual-diff-report和review-report生成器；这轮没有生成新图像资产，只复用已有本地素材。

## 本轮证据

- browser-run 19 PASS / 0 FAIL / 0错误 / 0 HTTP≥400；28主图+84响应式图。
- state-run 16 PASS；boundary-run 9 PASS；layout-run 10 PASS；catalog-run 8 PASS，均0失败/异常。
- 合计62项检查，不是62个完整业务场景。新增3项分别遍历双角色个人中心、7篇原文、审核三处理/长文/空态。
- 最新type-check、Rive、H5、mp-weixin、App构建均PASS，包含最终DetailIntro改动。
- 静态等价：原83锚点全保留，现171；原123命名函数全保留；仅closeDetail沿用前轮植物反馈文案变化。原216资源未删除/改写，6核心文件hash相同，原目录clean。
- 13/16/26最新运行图已实际打开观察；边界文案/多余入口保留使页面纵向长度和参考不同，不伪造相似度分数。
- 图册及汇总由现有脚本重新生成；详见latest目录。

## 下一个切片

1. 继续02运动前检查、10运动后状态、15训练数据，先打开各参考与运行图；按原字段/守卫/数据源做显示层密度优化。不要把参考新增字段推定为原必填项。
2. 扩展全局字号、触控、对比度、键盘的真实检查；当前局部≥11px和按钮≥44px不能外推为全站通过。
3. 运行资源审计：src/static约123M，mp输出约146M；视频53M和历史背景/分帧/分镜占比大。已经只读确认rive-motion引用3个训练视频，prototype-data引用3个知识视频，多个海报与紫喜master仍是运行资源。不能用“未直接搜到路径”就删资源，需考虑动态/Rive引用并保留原文件。
4. 微信/App真机、正式包体和full/handoff门禁仍缺证据；存在安全可做的本地工作，不是阻塞。

## 运行与边界

H5地址http://127.0.0.1:4173/；修改只限副本，无提交/推送/部署，无真实医院、健康数据、相机、支付和消息。
原项目/参考图只读；全绿UI、紫色小喜、无奖杯、树无土/盆/地台继续生效。当前目标保持active。
